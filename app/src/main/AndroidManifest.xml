<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.booked_me">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bookedme">
        <activity android:name=".presentation.feeds.AddBookActivity"></activity>
        <activity android:name=".presentation.feeds.MyFeedsActivity" /> <!-- <activity android:name=".profile.SettingProfileActivity"></activity> -->
        <!-- <activity android:name=".order.OrderDetailActivity" /> -->
        <!-- &lt;!&ndash; <activity android:name=".SplashScreenActivity" /> &ndash;&gt; -->
        <!-- <activity android:name=".order.OrderActivity" /> -->
        <!-- <activity android:name=".onboarding.OnBoardingActivity" /> -->
        <!-- <activity android:name=".CartActivity" /> -->
        <!-- <activity android:name=".SplashScreenActivity" /> -->
        <!-- <activity android:name=".DetailBookActivity" /> -->
        <!-- <activity android:name=".MainActivity"> -->
        <activity android:name=".presentation.profile.SettingProfileActivity" />
        <activity android:name=".presentation.order.OrderDetailActivity" />
        <activity android:name=".presentation.order.OrderActivity" />
        <activity android:name=".presentation.onboarding.OnBoardingActivity" />
        <activity android:name=".presentation.CartActivity" />
        <activity android:name=".presentation.DetailBookActivity" />
        <activity android:name=".presentation.MainActivity" />
        <activity android:name=".presentation.login_register.RegisterActivity" />
        <activity android:name=".presentation.login_register.LoginActivity" />
        <activity android:name=".presentation.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>